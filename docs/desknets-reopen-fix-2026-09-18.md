# DeskNet's予定追加画面の再表示修正

- 閉じたDeskNet'sタブを、同じ専用ブラウザのセッション内で作り直す。
- 予約フォームを再構築した場合は、保存済みの参加者・議題・日時・会議室・通知設定を復元して検証する。
- 専用Edge自体が終了している場合は既存の再起動処理から接続を再開する。ログインが切れている場合は手動ログインが必要。
- `/approve` は手動確定待ちの再表示と、承認後の失敗からの再試行を受け付ける。既存の所有者確認、15分の確認期限、開始時刻チェックは継続。
- AzureChat側も変更：`C:/Users/021213/azurechat-office-work/src/features/desknets-agent/desknets-approval-card.tsx`。手動確定待ちではオレンジの「DeskNet'sの予定追加画面を再表示」ボタンを残す。
- 最終登録はDeskNet's上の「追加」を利用者が押す。再表示時に会議室の予約状況を再確認する。

検証：`npm test`、AzureChat変更ファイルの `next lint`、差分の空白チェック成功。
追加ブラウザテストは外部接続しない模擬ページで、初回承認前に閉じたタブと手動確定待ちで閉じたタブを復元し、フォーム値と登録操作ゼロを確認した。実DeskNet'sでの操作確認は未実施。

ローカルAgent APIを再起動し、`http://127.0.0.1:3001/health` の `ok` を確認済み。再起動で候補は消えるため、AzureChatでは画面を更新して新しい検索から確認する。Azureへのデプロイはしていない。
